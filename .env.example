# Cloud SQL instance connection name: project-id:region:instance-name
CLOUD_SQL_INSTANCE=eff-dev-497918:us-central1:eff-dev-db

DB_NAME=mydb
DB_USER=appuser
DB_PASSWORD=changeme

# Set to "development" locally to enable debug features
APP_ENV=production
