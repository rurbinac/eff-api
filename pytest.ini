[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto

# Markers for organizing tests
markers =
    integration: integration tests that require database
    unit: unit tests with mocks/fixtures
    slow: slow running tests
